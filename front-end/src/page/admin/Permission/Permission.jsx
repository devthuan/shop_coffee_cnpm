import classNames from "classnames/bind";
import styles from "./Permission.module.scss";

const cx = classNames.bind(styles);
export const Permission = () => {
  return <div className={cx("")}>Permission welcome</div>;
};
