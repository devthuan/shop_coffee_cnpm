import classNames from "classnames/bind";
import styles from "./Bill.module.scss";

const cx = classNames.bind(styles);
export const Bill = () => {
  return <div className={cx("")}>Bill welcome</div>;
};
