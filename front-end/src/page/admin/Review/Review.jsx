import classNames from "classnames/bind";
import styles from "./Review.module.scss";

const cx = classNames.bind(styles);
export const Review = () => {
  return <div className={cx("")}>Review welcome</div>;
};
