import classNames from "classnames/bind";
import styles from "./Dashboard.module.scss";

const cx = classNames.bind(styles);
export const Dashboard = () => {
  return <div className={cx("")}>Dashboard product</div>;
};
