import classNames from "classnames/bind";
import styles from "./Order.module.scss";

const cx = classNames.bind(styles);
export const Order = () => {
  return <div className={cx("")}>Order welcome</div>;
};
