import classNames from "classnames/bind";
import styles from "./Discount.module.scss";

const cx = classNames.bind(styles);
export const Discount = () => {
  return <div className={cx("")}>Discount welcome</div>;
};
