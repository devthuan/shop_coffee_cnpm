import classNames from "classnames/bind";
import styles from "./Receipt.module.scss";

const cx = classNames.bind(styles);
export const Receipt = () => {
  return <div className={cx("")}>Receipt welcome</div>;
};
