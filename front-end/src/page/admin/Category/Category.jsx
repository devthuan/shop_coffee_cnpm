import classNames from "classnames/bind";
import styles from "./Category.module.scss";

const cx = classNames.bind(styles);
export const Category = () => {
  return <div className={cx("")}>Category welcome</div>;
};
