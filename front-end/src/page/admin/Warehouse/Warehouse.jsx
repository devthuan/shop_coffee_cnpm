import classNames from "classnames/bind";
import styles from "./Warehouse.module.scss";

const cx = classNames.bind(styles);
export const Warehouse = () => {
  return <div className={cx("")}>Warehouse welcome</div>;
};
